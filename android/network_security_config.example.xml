<?xml version="1.0" encoding="utf-8"?>
<!--
  Bu dosyayi su konuma kopyalayin:
  android/app/src/main/res/xml/network_security_config.xml

  ESP32 ve AI sunucu IP adreslerinizi asagidaki domain-config bloguna ekleyin.
  network_security_config.xml GitHub'a yuklenmez.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- AI sunucu (HTTP) — kendi IP veya domain -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">YOUR_AI_SERVER_IP</domain>
    </domain-config>

    <!-- ESP32 (yerel HTTP) — hotspot aginizdaki ESP IP -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">YOUR_ESP_IP</domain>
    </domain-config>

    <!-- Lokal gelistirme -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
