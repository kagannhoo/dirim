import 'package:flutter/material.dart';

class AppColors {
  // Dirim marka yeşili
  static const Color primaryBlue = Color(0xFF1E7A4C);
  static const Color accentGreen = Color(0xFF84CC16);

  // Göz yormayan, medikal hisli açık gri/beyaz
  static const Color backgroundWhite = Color(0xFFF4F6F9);

  // Formlar ve kartlar için saf beyaz
  static const Color surfaceWhite = Colors.white;

  // Başlıklar için tok bir gri
  static const Color textDarkGrey = Color(0xFF2C3E50);

  // Alt metinler için açık gri (HATA VEREN BUYDU, ŞİMDİ EKLENDİ)
  static const Color textLightGrey = Color(0xFF7F8C8D);

  // Hata mesajları ve Nabız ikonu için kırmızı (HATA VEREN BUYDU, ŞİMDİ EKLENDİ)
  static const Color errorRed = Color(0xFFE74C3C);
}
